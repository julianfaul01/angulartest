import { GamehoverDirective } from './gamehover.directive';

describe('GamehoverDirective', () => {
  it('should create an instance', () => {
    const directive = new GamehoverDirective();
    expect(directive).toBeTruthy();
  });
});
